{
  "name": "dicktaint",
  "version": "0.1.0",
  "private": true,
  "description": "Local dictation app with native Whisper CLI transcription and local model management",
  "scripts": {
    "start": "bun run server.js",
    "dev": "bun --watch server.js",
    "test": "bun test",
    "whisper:sidecar": "./scripts/build-whisper-sidecar.sh",
    "whisper:smoke": "./scripts/smoke-whisper-sidecar.sh",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "test:all": "bun run test && bun run test:rust",
    "tauri:dev": "bunx tauri dev",
    "tauri:build": "bunx tauri build",
    "tauri:android:init": "bunx tauri android init --ci",
    "tauri:android:dev": "HOST=0.0.0.0 PORT=43210 bunx tauri android dev",
    "tauri:android:run": "bunx tauri android run",
    "tauri:android:build": "bunx tauri android build",
    "tauri:ios:init": "bunx tauri ios init --ci",
    "tauri:ios:dev": "HOST=0.0.0.0 PORT=43210 bunx tauri ios dev",
    "tauri:ios:run": "bunx tauri ios run",
    "tauri:ios:build": "bunx tauri ios build"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
